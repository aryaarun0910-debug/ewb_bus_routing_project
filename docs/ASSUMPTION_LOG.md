# Assumption Log — UK2026-82 Predictive Bus Routing

> Rubric target: Criterion 2a, score 5 — *"identifying, verifying and logging assumptions in the team's decision-making."*
> This log names every load-bearing assumption in the pipeline, where it enters the code, its verification status, what happens if it is wrong, and the trigger that would force a revisit.
> Status legend: **VERIFIED** (tested against independent evidence) · **BOUNDED** (untestable directly, but sensitivity-tested and disclosed) · **OPEN** (named, monitored, not yet testable with available data).

| # | Assumption | Enters the pipeline at | Status | Evidence / bound | Impact if wrong | Revisit trigger |
|---|------------|------------------------|--------|------------------|-----------------|-----------------|
| A1 | The hour-of-day demand **shape** (commuter-peak curves) at Ladywood stops resembles standard UK urban boarding curves | `PROFILE_FN` in `generate_map_dataset.py`, reused by `generate_real_demand_dataset.py` | **OPEN** | Tested against the only public proxy — GTFS service frequency — yielding median Pearson 0.06; disclosed plainly in MODEL_CARD.md as a weak proxy, not a validation | Routes optimised for the wrong hours; peak buses arrive off-peak | TfWM APC data request answered, or manual stop survey completed |
| A2 | Relative per-stop demand today is **proportional** to 2010–2016 ENCTS concessionary smartcard volumes | `build_anchors()` in `generate_real_demand_dataset.py` | **BOUNDED** | ±20% perturbation of the anchor retrained: R² spread = 0.0004 (`robustness_analysis.py`) — headline accuracy is not an artefact of the anchor's exact magnitude | Mis-ranked stops; service skewed toward historically-busy stops | Any post-2016 patronage source at stop level becomes available |
| A3 | Demand structure is **stable across time** (the i.i.d./representativeness assumption of supervised learning) | Train/test split in `demand_route_optimizer.py` | **BOUNDED** | Temporal split (train 2023 → test 2024) reported as primary: R² 0.945 vs 0.949 random (gap 0.004); season-shift transfer 0.934; structural-break vulnerability named with COVID-scale magnitude (Instacart 93→61%) and CQR proposed as mitigation | A structural break (new tram line, regeneration, pandemic) silently degrades predictions | Rolling R² on any future observed data drops below the season-shift floor (0.934) |
| A4 | A bus serves ≤ 40 passengers at once; ≤ 320 across a 2–3 h window; routes fit a 70-minute one-way budget | `BUS_CAPACITY`, `ROUTE_BUDGET_MIN` in `demand_route_optimizer.py` | **VERIFIED** (operator specs) | Single-decker capacity from National Express WM fleet specifications; 70 min consistent with TfWM route 8A/8C cycle times | Overcrowding on under-capacity assumptions; infeasible schedules | Operator consultation in pilot phase |
| A5 | Predicted demand is **independent of the service offered** (no induced-demand feedback in the model) | Whole demand-model design | **OPEN** | Named here; the optimiser reallocates within a fixed fleet, so total service is conserved — bounding the feedback's first-order effect | Under-served stops appear "low demand" and get less service — a self-fulfilling loop | Pilot-phase boarding counts diverging from predictions at de-prioritised stops |
| A6 | Weather multipliers (rain suppresses discretionary trips, storms suppress most demand) transfer from literature to Ladywood | `WEATHER_DEMAND_MULT`, `CLIMATE_EVENT_DEMAND` in `generate_map_dataset.py` | **BOUNDED** | Directionally consistent with published UK transit-weather elasticities; magnitudes are modelled; season-shift check (0.934) bounds the cost of seasonal mis-calibration | Wrong storm-day allocation | Pilot observation of a named-storm day |
| A7 | Vehicle operating cost £4.47/km (DfT BUS0404 2022/23) and passenger time value £9.80/hr (DfT TAG A1.3 low-income working) apply | `analysis/cost_model.py` | **VERIFIED** (official sources) | DfT-published national methodology; ONS ASHE 2023 Ladywood wage used for the low-income rate | Savings mis-stated | Annual DfT statistical release updates |
| A8 | A 12.5% vehicle-km reduction translates ~linearly to fuel, CO₂ and NO₂/PM reductions on the same fleet | CO₂/air-quality claims (README, deck slide 18) | **BOUNDED** | Linear in km for a fixed Euro-VI fleet mix; cold-start and congestion second-order effects disclosed as unmodelled | Environmental benefit over-stated by second-order effects | Telematics data from pilot vehicles |
| A9 | Special events (festivals, closures) occur with the modelled frequency and demand multipliers | `SPECIAL_EVENT_*` tables in `generate_map_dataset.py` | **OPEN** | No public event log exists for these stops (disclosed in Caveats); synthetic overlays kept rare (≤14% of days) | Event-day routing sub-optimal | Council events API or community calendar integration |
| A10 | LoRa 433 MHz reaches all 15 stops from a central hub in dense urban Ladywood (with mesh relay where needed) | `docs/radio_signalling_report.md` (proposed live architecture) | **OPEN** | Link-budget reasoning + DFRobot mesh-relay fallback documented; ISM duty-cycle compliance (<10%, ≤10 mW ERP) verified against UK regulations | Some stops need relay nodes (+£15–20 each) | RF site survey in pilot phase |
| A11 | The dissimilarity index Σ\|service share − demand share\|/2 is the right equity metric | `analysis/equity.py` | **VERIFIED** (by elimination, documented) | Gini returned 0.0 for both routing types (identical static coverage); service÷demand-ratio Gini distorted by near-zero-demand stops; methodology note in `equity.py` records the full reasoning — an *identified, verified and logged* metric decision | Equity gain mis-measured | Resident-defined metrics may replace it (governance Phase 2) |

## How this log is used
Every entry follows the same loop the rubric asks for: **identify** (named here), **verify** (the Evidence column — six robustness checks, source citations, or explicit bounds), **log** (this file, version-controlled). Open assumptions are not weaknesses hidden in footnotes; they are the project's funded next steps (APC request, pilot survey, CQR intervals).
